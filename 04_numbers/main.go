	// === Basic Number Types ===
	// 1. Declare an integer variable with a value of 42.
	// 2. Declare a floating-point variable with a value of 3.14.
	// 3. Declare a complex number with real part 2 and imaginary part 3.
	// 4. Declare a variable of type `byte` and assign it a character.
	// 5. Declare a `rune` variable and print its Unicode code point.

	// === Basic Arithmetic & Bitwise Operations ===
	// 6. Perform addition, subtraction, multiplication, and division on two integers.
	// 7. Perform addition, subtraction, multiplication, and division on two floating numbers.
	// 8. Perform bitwise AND, OR, XOR, and NOT operations on two integers.
	// 9. Perform left shift and right shift operations on an integer.
	// 10. Demonstrate integer division and print the result of `5 / 2`.

	// === Type Conversions ===
	// 11. Convert an integer to a float.
	// 12. Convert a float to an integer.
	// 13. Convert a rune (Unicode code point) to an integer.
	// 14. Convert an integer to a string.
	// 15. Convert a byte slice to a string.

	// === Formatting Numbers ===
	// 16. Print an integer in binary format.
	// 17. Print an integer in hexadecimal format.
	// 18. Print an integer in octal format.
	// 19. Print a floating-point number with two decimal places.
	// 20. Print a floating-point number in scientific notation.

	// === Generating Random Numbers ===
	// 21. Generate a random integer between 1 and 100.
	// 22. Generate a random floating-point number between 0 and 1.
	// 23. Generate a random number within a specific range (10 to 50).
	// 24. Seed the random number generator with the current time.

	// === Math Functions (math Package) ===
	// 25. Calculate the absolute value of -10.
	// 26. Calculate the square root of 49.
	// 27. Calculate 2 raised to the power of 8.
	// 28. Find the minimum and maximum of two numbers.
	// 29. Round a floating-point number to the nearest integer.
	// 30. Use `Ceil()` and `Floor()` to round a float up and down.

	// === Working with Complex Numbers ===
	// 31. Declare a complex number `z = 3 + 4i`.
	// 32. Extract and print the real and imaginary parts of `z`.
	// 33. Add two complex numbers together.
	// 34. Multiply two complex numbers together.
	// 35. Find the absolute value (magnitude) of a complex number.

	// === Performance Considerations ===
	// 36. Declare an `int` variable without specifying the bit size.
	// 37. Convert a large integer to `int32` and observe what happens.
	// 38. Use the `math/big` package to handle very large numbers.
	// 39. Avoid unnecessary float-to-int conversions by using correct data types.
	// 40. Discuss why `int` is usually preferable over `int8` or `int64` unless necessary.
