// === Basic Enums Using `iota` ===
// 1. Define an enum for the days of the week using `iota`.
// 2. Define an enum for different user roles (`Admin`, `User`, `Guest`) using `iota`.
// 3. Print the values of the defined enums to verify their assignment.

// === Custom Values for Enum Constants ===
// 4. Define an enum where values start at 100 and increment using `iota`.
// 5. Define an enum where only some constants are assigned explicit values.

// === String Conversion for Enums ===
// 6. Write a function that converts an enum value to a string representation.
// 7. Write a function that converts a string back to an enum value.

// === Using Enums in Structs ===
// 8. Define a struct `User` with a `Role` field of type enum.
// 9. Create a function that accepts a `User` and prints their role.
// 10. Create and print different `User` structs with different roles.

// === Iterating Over Enums ===
// 11. Store enum values in a slice and iterate over them.
// 12. Use a map to associate enum values with descriptions and iterate over it.

// === Bitwise Flags Using Enums ===
// 13. Define bitwise flags using `iota` (`Read`, `Write`, `Execute`).
// 14. Combine multiple flags using bitwise OR (`|`).
// 15. Check if a specific flag is set using bitwise AND (`&`).

// === Performance Considerations ===
// 16. Compare using an `int` enum vs. using a `string` for performance.
// 17. Show why using `iota` can reduce errors in manual constant assignments.
