// === Basic Array Operations ===
// 1. Declare an array of 5 integers and assign values to it.
// 2. Declare an array of strings with initial values: "Go", "Rust", "Python".
// 3. Access and print the third element of an array.
// 4. Modify the second element of an array and print the entire array.
// 5. Find and print the length of an array using `len()`.

// === Basic Slice Operations ===
// 6. Declare a slice of integers with initial values: 10, 20, 30.
// 7. Create a slice from an existing array.
// 8. Access and print the first and last elements of a slice.
// 9. Append values to a slice and print the result.
// 10. Append multiple values at once to a slice.

// === Slice Manipulation ===
// 11. Create a slice of numbers and use `copy()` to duplicate it.
// 12. Use slicing to extract elements from a slice (`slice[1:3]`).
// 13. Use `len()` and `cap()` to check the length and capacity of a slice.
// 14. Use `make()` to create a slice with an initial capacity of 10.
// 15. Demonstrate how slice capacity grows dynamically when appending elements.

// === Iterating Over Arrays & Slices ===
// 16. Iterate over an array using a `for` loop and print each element.
// 17. Iterate over a slice using `range` and print index-value pairs.
// 18. Iterate over a slice and modify elements in-place.
// 19. Reverse a slice in-place.
// 20. Remove the second element from a slice without leaving an empty space.

// === Multidimensional Arrays & Slices ===
// 21. Declare a 2x3 array of integers and initialize it with values.
// 22. Access and print an element from a multidimensional array.
// 23. Create a slice of slices (jagged array) and initialize values.
// 24. Append a new row to a slice of slices.
// 25. Print a multidimensional array in matrix format.

// === Performance Considerations ===
// 26. Show how modifying a slice affects the underlying array.
// 27. Create a slice with `make()` and preallocate capacity to avoid reallocation.
// 28. Use `append()` to grow a slice beyond its original capacity and observe what happens.
// 29. Compare passing an array vs. passing a slice to a function in terms of performance.
// 30. Show how `copy()` can be used to efficiently move data in slices.
