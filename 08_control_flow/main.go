
// === If-Else Statements ===
// 1. Write an `if` statement that prints "Positive" if a number is greater than 0.
// 2. Extend the `if` statement to also print "Negative" if the number is less than 0.
// 3. Use `if` with a short variable declaration inside the condition.
// 4. Write an `if-else if-else` chain that checks if a number is positive, negative, or zero.

// === Switch Statements ===
// 5. Write a `switch` statement that prints the day of the week based on an integer input (1-7).
// 6. Use `switch` without an explicit condition to handle multiple cases.
// 7. Use `fallthrough` in a `switch` statement and explain its effect.
// 8. Use a `switch` statement on a variable of type `interface{}` and use a **type switch**.
// 9. Modify the **type switch** to handle `int`, `string`, and `bool` cases separately.

// === For Loops ===
// 10. Write a `for` loop that prints numbers from 1 to 10.
// 11. Use a `for` loop to calculate the sum of numbers from 1 to 100.
// 12. Write a `for` loop that iterates over a slice and prints each element.
// 13. Iterate over a map and print both keys and values.
// 14. Use a `for` loop with `range` to print each character in a string.
// 15. Write an infinite loop using `for` and break out of it with `break`.
// 16. Write a loop that continues to the next iteration when the number is even.

// === Break, Continue, and Goto ===
// 17. Use `break` to exit a loop when a certain condition is met.
// 18. Use `continue` to skip an iteration of a loop.
// 19. Write a `goto` statement to jump to a labeled statement.

// === Defer Statements ===
// 20. Use `defer` to print "Function End" at the end of `main()`, regardless of where `return` occurs.
// 21. Demonstrate how multiple `defer` calls execute in **LIFO (Last In, First Out)** order.

// === Panic and Recover ===
// 22. Create a function that triggers a `panic` and use `recover` to catch it.
// 23. Explain why `panic` should be used only for unrecoverable errors.
// 24. Use `defer` with `recover` to gracefully handle an error.
