// === Basic Pointers ===
// 1. Declare a pointer to an integer and assign it a value.
// 2. Print the memory address of an integer variable using `&`.
// 3. Dereference a pointer to access its value.

// === Pointers and Functions ===
// 4. Write a function that takes an `int` pointer and doubles its value.
// 5. Modify a string inside a function using a pointer.

// === Pointers and Structs ===
// 6. Define a struct `Person` with fields `Name` and `Age`.
// 7. Create a pointer to a `Person` struct and modify its fields.
// 8. Write a function that takes a pointer to `Person` and updates its `Age`.

// === Nil Pointers and Default Values ===
// 9. Declare a pointer without initializing it and print its value.
// 10. Check if a pointer is `nil` before dereferencing it.

// === `new()` vs `make()` ===
// 11. Use `new()` to allocate memory for an integer.
// 12. Use `make()` to initialize a slice and compare it with `new()`.

// === Pointer Receivers in Methods ===
// 13. Define a struct `Counter` with an `int` field.
// 14. Create a method `Increment()` with a pointer receiver that increments the counter.
// 15. Explain why a pointer receiver is needed in `Increment()`.

// === Advanced Pointer Concepts ===
// 16. Create an array and use a pointer to iterate over its elements.
// 17. Create a slice and modify its contents using a pointer.
// 18. Demonstrate why Go **does not** support pointer arithmetic like C.
